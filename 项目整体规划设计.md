# 通用数据分析Multi-Agent系统项目规划设计

## 1. 项目需求和目标分析

### 1.1 系统需要处理的数据源类型
- **结构化数据**：xlsx、csv、xls等表格文件，通常包含表头和大量数据（一般在百万行以下）
- **非结构化数据**：markdown、doc等文件，以及带有各种格式、合并单元格、图片信息的复杂表格文件
- **数据源描述文件**：用于介绍数据源文件的细节信息

### 1.2 数据源处理策略
- 对于规范化数据（如csv、xlsx、xls）：读取表头，均匀抽样读取几十行数据来了解文件结构
- 对于非规范化数据（如markdown、doc）：如果长度不是特别长（十几万字以下），可以读取全文进行分析

### 1.3 项目背景文件的作用和内容结构
**作用**：
- 作为纲领性文件，描述项目的背景信息、分析目标、关键指标和期望结论
- 为分析思路规划Agent提供核心指导信息

**内容结构**：
- 项目背景信息
- 分析目标
- 分析的关键指标
- 希望得到的分析结论

### 1.4 项目上下文文件的作用和内容结构
**作用**：
- 由项目主流程Agent维护
- 管理记录项目运行的全局上下文信息和运行状态
- 作为共享资源提供给所有Agent参考

**内容结构**：
- 项目运行的全局上下文信息
- 项目运行状态
- 各Agent间需要共享的信息

### 1.5 各类Agent的职责和交互关系
1. **项目主流程Agent**：把控项目整体进度，分配各Agent工作，维护全局项目上下文文件，与用户交流
2. **数据源文件分析Agent**：读取数据源，构建描述文件，实现对数据源的精准描述
3. **分析思路规划Agent**：根据数据源描述文件和项目背景文件，细化出分析规划文件
4. **思路验证Agent**：评估验证分析思路，与用户交流确认合理性
5. **代码结构设计Agent**：根据分析规划文件生成代码设计文件
6. **分析执行Agent**：基于代码设计文件编写和执行notebook代码
7. **结果验证Agent**：验证分析结果，处理异常情况

### 1.6 系统的整体目标和关键功能点
**整体目标**：
构建一个通用数据分析Multi-Agent系统，能够适应各种场合的数据分析任务，通过多个专门Agent的协作，实现从数据源分析到结果验证的完整流程自动化。

**关键功能点**：
1. 多格式数据源支持（xlsx、csv、xls、markdown等）
2. 项目背景和上下文管理
3. 多Agent协作机制
4. 分析思路规划和验证
5. 自动化代码生成和执行（基于Jupyter Notebook）
6. 分析结果验证和异常处理
7. 用户交互机制，确保结果符合用户需求
8. 柔性的Agent间通信schema设计，在约束和通用性之间取得平衡

## 2. 项目整体架构设计

### 2.1 项目目录结构设计
```
D:\Desktop\data\数据复盘\Claude Code Auto Analysis\  # 项目根目录
├── CLAUDE.md                                       # 项目配置文件
├── README_nb_runner.md                             # Notebook运行器使用说明
├── nb_runner.py                                    # Notebook运行器脚本
├── agents/                                         # Agents模块目录
│   ├── __init__.py
│   ├── master_agent.py                            # 项目主流程Agent
│   ├── data_source_analyzer.py                    # 数据源分析Agent
│   ├── planning_agent.py                          # 分析思路规划Agent
│   ├── validation_agent.py                        # 思路验证Agent
│   ├── code_design_agent.py                       # 代码结构设计Agent
│   ├── execution_agent.py                         # 分析执行Agent
│   └── result_validation_agent.py                 # 结果验证Agent
├── config/                                         # 配置文件目录
│   ├── project_context.json                       # 项目上下文文件
│   └── agent_config.json                          # Agent配置文件
├── data/                                           # 数据源目录
│   ├── raw/                                       # 原始数据文件
│   ├── processed/                                 # 处理后的数据文件
│   └── descriptions/                              # 数据源描述文件
├── docs/                                           # 文档目录
│   ├── project_background.md                      # 项目背景文件
│   ├── analysis_plans/                            # 分析规划文件目录
│   └── code_designs/                              # 代码设计文件目录
├── scripts/                                        # 辅助脚本目录
│   ├── data_readers/                              # 数据读取脚本
│   └── utils/                                     # 工具脚本
├── templates/                                      # 模板目录
│   └── notebook_template.ipynb                    # Notebook模板文件
└── logs/                                           # 日志目录
```

### 2.2 数据流和控制流设计
**数据流设计**：
```
[原始数据文件] → [数据源分析Agent] → [数据源描述文件] → 
[分析思路规划Agent] → [分析规划文件] → 
[代码结构设计Agent] → [代码设计文件] → 
[分析执行Agent] → [Jupyter Notebook执行] → [分析结果] → 
[结果验证Agent] → [验证报告] → (如有异常则重新执行)
```

**控制流设计**：
```
项目启动
    ↓
主流程Agent读取项目背景文件
    ↓
主流程Agent创建项目目录结构和上下文文件
    ↓
数据源分析Agent分析所有数据文件 → 生成数据源描述文件
    ↓
分析思路规划Agent结合背景文件和数据描述 → 生成分析规划文件
    ↓
思路验证Agent验证分析规划 ←→ 用户交互确认
    ↓
代码结构设计Agent将分析规划转化为代码设计文件
    ↓
分析执行Agent根据代码设计生成Notebook并执行
    ↓
结果验证Agent验证执行结果 ←→ 判断是否需要重新执行
    ↓
主流程Agent汇总所有分析结果 → 生成最终报告
```

### 2.3 并发设计
1. **数据源分析阶段**：可以并行分析多个不同类型的数据文件
2. **分析规划阶段**：针对不同的分析目标可以并行制定规划
3. **代码执行阶段**：不同的分析任务可以并行执行（在资源允许的情况下）
4. **结果验证阶段**：可以并行验证多个分析结果

### 2.4 Jupyter Notebook使用规范
**Notebook创建规范**：
1. 命名规范：每个分析规划文件对应一个Notebook，命名格式：`分析目标_时间戳.ipynb`
2. 结构规范：
   ```
   Cell 1: 导入必要的库和设置
   Cell 2: 加载和预处理数据
   Cell 3-N: 具体的分析步骤
   Cell N+1: 结果可视化
   Cell N+2: 结论总结
   ```

**目录管理规范**：
1. 项目目录创建：根据项目背景文件中的项目名称，在Jupyter根目录创建同名工程目录
2. 文件组织：
   ```
   D:\Program\jupyter\项目名称\
   ├── analysis_notebooks\          # 分析用的Notebook文件
   ├── result_notebooks\            # 结果展示用的Notebook文件
   ├── data\                        # 项目专用数据文件
   ├── images\                      # 生成的图表图片
   └── reports\                     # 生成的报告文件
   ```

## 3. Agent间通信Schema设计

### 3.1 项目上下文信息结构和更新机制
```json
{
  "project_id": "uuid",
  "project_name": "string",
  "project_root": "string",
  "notebook_root": "string",
  "created_at": "datetime",
  "updated_at": "datetime",
  "status": "enum[pending, running, paused, completed, failed]",
  "current_phase": "enum[data_source_analysis, planning, validation, code_design, execution, result_validation]",
  "progress": {
    "total_steps": "integer",
    "completed_steps": "integer",
    "current_step": "string"
  },
  "agents": {
    "active_agents": ["agent_id"],
    "completed_agents": ["agent_id"]
  },
  "data_sources": {
    "processed": ["source_id"],
    "unprocessed": ["source_id"]
  },
  "analysis_plans": {
    "validated": ["plan_id"],
    "pending_validation": ["plan_id"]
  },
  "errors": [
    {
      "agent_id": "string",
      "timestamp": "datetime",
      "error_type": "string",
      "description": "string",
      "severity": "enum[low, medium, high, critical]"
    }
  ],
  "user_feedback": [
    {
      "timestamp": "datetime",
      "content": "string",
      "related_to": "string"
    }
  ]
}
```

### 3.2 各类文件标准化格式
**数据源描述文件**：
```json
{
  "source_id": "uuid",
  "file_name": "string",
  "file_path": "string",
  "file_type": "enum[csv, xlsx, xls, markdown, doc, other]",
  "size": "integer",
  "created_at": "datetime",
  "last_modified": "datetime",
  "description": "string",
  "structure": {
    "row_count": "integer",
    "column_count": "integer",
    "columns": [
      {
        "name": "string",
        "type": "string",
        "sample_values": ["any"]
      }
    ]
  },
  "metadata": {
    "encoding": "string",
    "delimiter": "string",
    "has_header": "boolean"
  },
  "quality": {
    "completeness": "float",
    "consistency": "float",
    "accuracy": "float"
  },
  "tags": ["string"]
}
```

**分析规划文件**：
```json
{
  "plan_id": "uuid",
  "title": "string",
  "description": "string",
  "related_objectives": ["objective_id"],
  "related_indicators": ["indicator_id"],
  "related_outcomes": ["outcome_id"],
  "data_sources": ["source_id"],
  "methodology": "string",
  "steps": [
    {
      "step_id": "string",
      "title": "string",
      "description": "string",
      "expected_output": "string"
    }
  ],
  "validation_criteria": ["string"],
  "estimated_complexity": "enum[low, medium, high]",
  "estimated_duration": "integer(minutes)"
}
```

### 3.3 错误信息和状态信息传递格式
**错误信息格式**：
```json
{
  "error_id": "uuid",
  "agent_id": "string",
  "timestamp": "datetime",
  "error_type": "enum[validation_error, execution_error, data_error, system_error, user_error]",
  "severity": "enum[low, medium, high, critical]",
  "component": "string",
  "message": "string",
  "details": {
    "error_code": "string",
    "stack_trace": "string",
    "context": "object"
  },
  "suggested_action": "string",
  "retry_count": "integer"
}
```

## 4. Agent执行流程设计

### 4.1 各Agent的执行顺序和依赖关系
```
项目主流程Agent → 数据源文件分析Agent → 分析思路规划Agent → 思路验证Agent
                                          ↓
                                  代码结构设计Agent → 分析执行Agent → 结果验证Agent
                                          ↑              ↓
                                  用户交互 ← ← ← ← ← ← ← ← ← ← ← ← ← ← ← ←
```

### 4.2 并发执行设计
1. **数据源并行处理**：数据源文件分析Agent可并发处理多个不同类型的数据源文件
2. **分析任务并行执行**：对于多个分析规划文件，可并行启动多个分析执行Agent实例
3. **验证任务并行执行**：结果验证Agent可并发验证多个分析结果

### 4.3 任务调度机制
1. **任务队列管理**：主任务队列存储所有待执行的主要任务，子任务队列存储每个主要任务分解后的子任务
2. **优先级调度**：基础数据处理任务具有最高优先级，相互独立的分析任务可并行执行
3. **动态调度**：根据系统资源动态调整并发任务数量，根据任务执行情况动态调整任务优先级

### 4.4 异常处理和重试机制
1. **异常分类处理**：数据异常、执行异常、系统异常分别处理
2. **重试机制**：指数退避算法，最大重试次数3次
3. **人工介入**：超过最大重试次数后通知用户人工处理

## 5. 各Agent详细实现方案

### 5.1 项目主流程Agent
**职责**：
- 初始化项目环境，创建必要的目录结构
- 协调各Agent的执行顺序和任务分配
- 维护全局项目上下文文件
- 与用户进行交互，收集反馈

**输入输出**：
- 输入：项目背景文件、数据源文件
- 输出：全局项目上下文文件、项目目录结构

**关键逻辑**：
1. 读取项目背景文件，解析项目信息
2. 创建项目目录结构
3. 初始化项目上下文文件
4. 按顺序调用其他Agent执行任务
5. 监控任务执行状态
6. 汇总分析结果，生成最终报告

**异常处理**：
- 项目背景文件缺失或格式错误时，提示用户重新提供
- 目录创建失败时，记录错误日志并尝试重新创建

### 5.2 数据源文件分析Agent
**职责**：
- 读取各种格式的数据源文件
- 分析数据结构，生成数据源描述文件
- 对数据质量进行初步评估

**输入输出**：
- 输入：原始数据文件、项目上下文文件
- 输出：数据源描述文件

**关键逻辑**：
1. 根据文件扩展名识别文件类型
2. 调用相应的数据读取脚本读取文件内容
3. 分析数据结构（列名、数据类型、样本数据等）
4. 评估数据质量（完整性、一致性、准确性）
5. 生成标准化的数据源描述文件

**异常处理**：
- 文件读取失败时，记录错误信息并跳过该文件
- 数据格式不规范时，尝试数据清洗或标记为异常

### 5.3 分析思路规划Agent
**职责**：
- 根据项目背景文件和数据源描述文件制定分析规划
- 生成详细的分析步骤和方法论

**输入输出**：
- 输入：项目背景文件、数据源描述文件
- 输出：分析规划文件

**关键逻辑**：
1. 解析项目背景文件中的分析目标和关键指标
2. 结合数据源描述文件中的数据结构信息
3. 制定详细的分析步骤和方法论
4. 确定每个分析任务所需的数据源
5. 生成标准化的分析规划文件

**异常处理**：
- 数据源不足以支持某个分析目标时，标记该目标为不可行
- 分析目标描述不清晰时，请求用户提供更多信息

### 5.4 思路验证Agent
**职责**：
- 验证分析思路规划的合理性和可行性
- 与用户交互，确认分析规划符合需求

**输入输出**：
- 输入：分析规划文件、项目背景文件、数据源描述文件
- 输出：验证结果、用户反馈

**关键逻辑**：
1. 检查分析规划是否覆盖了所有项目目标
2. 验证分析方法是否适合数据特征
3. 评估分析规划的可行性和复杂度
4. 与用户交互，展示分析规划并收集反馈
5. 根据用户反馈调整分析规划

**异常处理**：
- 用户拒绝某个分析规划时，重新制定该部分规划
- 分析规划存在重大缺陷时，标记为需要重新设计

### 5.5 代码结构设计Agent
**职责**：
- 将分析规划转化为具体的代码设计方案
- 生成Notebook的代码结构和执行步骤

**输入输出**：
- 输入：分析规划文件
- 输出：代码设计文件

**关键逻辑**：
1. 解析分析规划文件中的分析步骤
2. 将每个步骤转化为具体的代码实现方案
3. 设计Notebook的代码结构（Cell划分）
4. 确定每个Cell的功能和依赖关系
5. 生成标准化的代码设计文件

**异常处理**：
- 分析步骤无法转化为代码时，标记为需要人工处理
- 代码设计过于复杂时，建议拆分为多个子任务

### 5.6 分析执行Agent
**职责**：
- 根据代码设计文件生成和执行Notebook代码
- 监控执行过程，处理执行异常

**输入输出**：
- 输入：代码设计文件、数据源文件
- 输出：执行完成的Notebook文件

**关键逻辑**：
1. 根据代码设计文件创建Notebook
2. 逐个Cell生成代码并执行
3. 监控每个Cell的执行结果
4. 记录执行过程中的输出和错误
5. 保存执行完成的Notebook文件

**异常处理**：
- 代码执行错误时，记录错误信息并尝试修复
- 执行超时时，中断执行并报告异常

### 5.7 结果验证Agent
**职责**：
- 验证分析结果的正确性和合理性
- 处理异常结果，触发重新执行流程

**输入输出**：
- 输入：执行完成的Notebook文件、数据源文件
- 输出：验证结果、重新执行信号

**关键逻辑**：
1. 读取Notebook执行结果
2. 验证结果是否符合预期
3. 检查结果中是否存在异常点
4. 生成验证报告
5. 必要时触发重新执行流程

**异常处理**：
- 结果异常时，删除异常分析结果并重新执行
- 验证失败次数过多时，标记为需要人工处理